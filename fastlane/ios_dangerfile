# BUILD_URL = ENV['build_url']
# BUILD_ID = ENV['build_id']

# Sometimes it's a README fix, or something like that - which isn't relevant for
# including in a project's CHANGELOG for example
declared_trivial = github.pr_title.include? "SwiftImageViewer PR Analysis"

# Make it more obvious that a PR is a work in progress and shouldn't be merged yet
warn("PR is classed as Work in Progress") if github.pr_title.include? "[WIP]"

# Warn when there is a big PR
# warn("Big PR") if git.lines_of_code > 500

# xcode_summary.report 'fastlane/test_output/report.junit'

test_results = 'fastlane/test_output/report.html'

message << declared_trivial

message << test_results
message << "Test results output should be above this comment."
# Don't let testing shortcuts get into master by accident
# fail("fdescribe left in tests") if `grep -r fdescribe specs/ `.length > 1
# fail("fit left in tests") if `grep -r fit specs/ `.length > 1
markdown << message
